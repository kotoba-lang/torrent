{:paths ["src"]
 :deps {
        ;; kotoba.lang.edn replaced clojure.edn here (2026-09-23). An explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, and kbb resolves git deps
        ;; only from nbb.edn, so every project file this repo has names it.
        io.github.kotoba-lang/edn {:git/sha "79dba32c8892058842c57783f06a2dfe4601aeb0"}}
 :aliases
 {:test
  {:extra-paths ["test" "../bencode/src" "../hash/src"]
   :extra-deps
   {io.github.kotoba-lang/kotoba {:local/root "../kotoba"}
    io.github.kotoba-lang/kotoba-core-contracts
    {:local/root "../kotoba-core-contracts"}
    io.github.kotoba-lang/kotoba-lang {:local/root "../kotoba-lang"}}
   :main-opts ["-m" "kotoba.torrent-v1-test"]}}}
