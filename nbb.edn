;; nbb.edn -- read by the kbb engine (kbb -M:test, kbb --backend sci).
;; kbb resolves NO git coordinate from deps.edn or bb.edn, only from here.
;; Written 2026-09-23 when kotoba.lang.edn replaced clojure.edn in this
;; repository. :deps only -- no :paths, so this file adds a dependency and
;; chooses no classpath.
{:deps {
        ;; kotoba.bytes replaced UTF-8 .getBytes / String. here (2026-09-24). An explicit
        ;; floor: tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/bytes {:git/sha "d5259f35ab7c4f5a66e63ca29ed4dcf7ba3f1ac5"}
        ;; kotoba.test (clojure.test-compatible surface), not clojure.test --
        ;; scripts/migrate-clojure-test-to-kotoba-test.cljk. Explicit floor.
        io.github.kotoba-lang/test {:git/url "https://github.com/kotoba-lang/test.git"
                                    :git/sha "2ab4885f99fd25552d380d0f6b0b54c5a72eb7ae"}io.github.kotoba-lang/edn {:git/sha "79dba32c8892058842c57783f06a2dfe4601aeb0"}}}
